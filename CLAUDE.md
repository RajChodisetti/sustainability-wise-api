@AGENTS.md

Before editing a target directory, also locate and read the nearest nested
`AGENTS.md` files. Use `npm run ai:context -- <feature-or-path>` to discover the
required business contracts and verification commands.

